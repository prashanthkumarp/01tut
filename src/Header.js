const Header = () => {
  const headstyle = {
    backgroundColor: "royalblue",
    color: "#fff",
  };
  return (
    <header style={headstyle}>
      <h1>Welcome to My React World</h1>
    </header>
  );
};

export default Header;
